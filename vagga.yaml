containers:
  projecty:
    setup:
    - !Ubuntu trusty
    - !UbuntuUniverse
    - !Install [libpq-dev, ca-certificates, libjpeg-dev, strace]
    - !Py3Requirements requirements.txt
    - !NpmInstall [gulp]

commands:
  run: !Command
    container: projecty
    run: |
        gulp
